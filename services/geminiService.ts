
// AI Service - Placeholder (AI features disabled)

export class GeminiService {
  async breakdownTask(_taskTitle: string): Promise<string[]> {
    // AI breakdown disabled - return empty array
    return [];
  }
}

export const geminiService = new GeminiService();
